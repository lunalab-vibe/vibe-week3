# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current state

This repository is currently a bare scaffold: it contains only `README.md` (whose sole content is the title `# vibe-week3`). There is no source code, build system, dependency manifest, test suite, or lint/CI configuration yet.

As the project takes shape, update this file with:
- Build, run, lint, and test commands (including how to run a single test) once a toolchain is chosen.
- The high-level architecture — the big-picture structure that spans multiple files and isn't obvious from a quick directory scan.

## Conventions

- 사용자와의 모든 대화(응답, 설명)는 한국어로 작성한다.
- 모든 커밋 메시지는 한국어로 작성한다.

## Design

- 버튼은 연보라(`#bb8cff`) 배경에 둥근 모서리(`border-radius: 12px`)를 적용한다.
